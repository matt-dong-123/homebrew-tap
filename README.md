# Matt-dong-123 Tap

## How do I install these formulae?

`brew install matt-dong-123/tap/<formula>`

Or `brew tap matt-dong-123/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "matt-dong-123/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
